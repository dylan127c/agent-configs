# https://mihomo.party/docs/guide/override/yaml

payload:
  # （VMWare）解决 CentOS 7 在添加 docker-ce 源时可能出现的网络连通性问题（需要为 yum 提供代理服务）
  # https://download.docker.com/linux/centos/docker-ce.repo
  # - "+.download.docker.com" # 匿名应用，无法使用 PROCESS-NAME 匹配，存在大量下载请求

  # （VMWare）解决 Docker 在拉取镜像时可能出现的网络连通性问题
  - "+.docker.io" # 匿名应用，无法使用 PROCESS-NAME 匹配，存在大量下载请求
  - "+.rubygems.org" # Logstash 安装插件请求的地址，com.docker.backend.exe 无法匹配规则
  - "+.segment.io"
  - "+.docker.com"
  - "+.dockerhub.com"