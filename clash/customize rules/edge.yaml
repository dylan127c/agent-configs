payload:
  # 以下域名用于加载Microsoft Edge新标签页，它们可能影响浏览器的加载速度。

  # 默认情况下，以下域名使用REJECT规则，这有可能可以加快浏览器的启动速度。

  # 如果遇到新标签页无法正常加载的情况，请将规则切换成DIRECT或PROXY，
  # 以帮助浏览器建立新的新标签页Cookies数据。

  - '+.ntp.msn.cn'
  - '+.assets.msn.cn'
  - '+.assets2.msn.cn'